import styled from 'styled-components';

export const StyledPokemonInfoCard= styled.div`
  padding: 10px 0;

  p {
    margin: 0;
    font-size: 12px;
    color: var(--drak-gray);
  }

  h3 {
    margin: 0;
  }
`
