import styled from 'styled-components';

export const StyledPokemonDetailImage = styled.figure`
  margin: 20px;
  padding: 20px;
  height:50%;
  border-radius: 5px;
  background-color: var(--background-element);
  box-shadow: 0px 2px 10px 0px var(--shadow);
  display: flex;
  justify-content: center;
  align-items: center;

  img {
    width: 50%
  }
`
